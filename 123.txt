#include <stdio.h>
struct student{
    char* name; // 姓名
    int num; // 学号
    int age; // 年龄
    float score;
};
int main(){
    int i = 0;
    int num_140=0;
    float sum=0;
    student Li={"Li ping",5,18,145.0};
    if(Li.score<140) {
        flag = -1;
    }
    else {
        flag=1;
    }
    // printf("%d\n",flag);
    // printf("%s\n",Li.name);
}